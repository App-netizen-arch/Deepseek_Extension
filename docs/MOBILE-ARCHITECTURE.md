# Mobile Architecture

The current Better DeepSeek product targets the browser extension and local runtime. The former native Android/Kotlin implementation is intentionally retired.

When mobile development begins, the companion will be implemented separately with Flutter/Dart rather than reintroducing the native Android application into this repository.

The future mobile companion may connect to the same authenticated local runtime where supported, but it is not part of the current release gate.
